export function parseConversationResponse(jsonText) {
  return JSON.parse(jsonText);
}

export function parseMessagesResponse(jsonText) {
  return JSON.parse(jsonText);
}

export function parseUserResponse(jsonText) {
  return JSON.parse(jsonText);
}

export function parseTenantsResponse(jsonText) {
  return JSON.parse(jsonText);
}
